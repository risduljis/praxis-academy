function timesTwo(params) 
{ 
    return params * 2 
} 
timesTwo(4);

var timesTwo = params => params * 2

timesTwo(4);

var feedTheCat = (cat) => {
    if (cat === 'hungry') {
      return 'Feed the cat';
    } else {
      return 'Do not feed the cat';
    }
  }

  var addValues = (x, y) => {
    return x + y
  }